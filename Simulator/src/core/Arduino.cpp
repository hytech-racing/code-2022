// #include "Arduino.h"

// bool digitalRead(int pin) 					{ return Simulator::io[pin].vehicle_read(); }
// unsigned analogRead(int pin)				{ return Simulator::io[pin].vehicle_read(); }
// void digitalWrite(int pin, bool value) 		{ Simulator::io[pin].vehicle_write(value); }
// void analogWrite(int pin, unsigned value)	{ Simulator::io[pin].vehicle_write(value); }
// void pinMode(int pin, unsigned mode) 		{ Simulator::io[pin].vehicle_pinMode(mode); }

// unsigned long long millis()					{ return Simulator::sys_time; }
// void delay (unsigned long long time) 		{ Simulator::sys_time += time; }